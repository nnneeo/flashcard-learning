# Flashcard Learning App

## Setup

**Requirements:** Python 3, Node.js, MongoDB running on localhost:27017

Backend:
```
cd backend
pip install -r requirements.txt
python -m uvicorn main:app --reload
```

Frontend:
```
cd frontend
npm install
npm run dev
```

Open http://localhost:5173
